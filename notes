missing if \n in between maps line check;

check 1 || 0 || ' '
if not
check if coord;

enclosed map ::
after space and before space should be 1;
if last element should be 1;
if first element and not space should be 1;
last line either 1 or space;
first line etierh 1 or space;